#pragma once
#include <string>
#include <functional>

namespace cppble
{

// class adapter {
// private:
//     std::string name;
//     explicit adapter(const std::string& name): name(name) {}
//
// public:
//     virtual ~adapter() = default;
//     virtual void scan(const std::function<void(std::string_view)>& onDeviceAppear) = 0;
//
// #ifdef CPPBLE_WINDOWS_OS
//     friend class WinAdapter;
// #endif
// };

}